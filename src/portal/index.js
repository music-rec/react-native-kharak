import Gateway from './Gateway';
import Black from './GatewayDest';
import Provider from './GatewayProvider';
import Registry from './GatewayRegistry';

export default { Provider, Registry, Black, Gateway };
